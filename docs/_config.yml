# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: The Network Entropologist
author: The Network Entropologist
email: 
description: >- # this means to ignore newlines until "baseurl:"
  <strong>Network Entropology (n.):</strong> The study of chaos in data network systems; the discipline concerned with 
          understanding how order degrades, complexity accumulates, and entropy propagates across connected infrastructure, 
          and the practice of bringing order back to it. A field that exists whether or not its practitioners know they are in it.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
# twitter_username: jekyllrb
github_username:  NetworkEntropologist

# Build settings
# theme: minima
remote_theme: NetworkEntropologist/jekyll-theme-bootstrap-minimal
plugins:
- jekyll-remote-theme
# plugins:
  # - jekyll-feed

permalink: /:year/:month/:day/:title:output_ext

logo: /assets/img/logo-graphic-light.png

# Edit your optional site keywords here, separated by commas. These are used in the meta tag for search engines.
keywords: 

# The relative path to the site favicon
favicon: 

# The relative path to the Applie Touch Icon
apple_touch_icon:

# These options control visibility of various elements in the theme.
show_sidebar: true
show_footer: true
show_menu: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
